# Beads Discussion #276 "Beads UI — make your pick" — comment scaffold

**Stage:** official. **Where:** the GitHub Discussion #276 in `gastownhall/beads`
(a thread where people list/showcase their Beads UIs). **When:** after public +
green + feedback pass.

**Tone:** one of many tools in a friendly roundup thread. Short, link-forward,
zero hard sell. This is a "make your pick" thread — describe, let people choose.

---

## Draft comment (tune voice; keep it short)

> Adding **beads.nvim** to the pile for the Neovim crowd 👋
>
> It's a Telescope front-end for `bd`: a filterable issue browser, a floating
> detail view with in-place dependency navigation, a dependency graph, epic
> children/status, change history, a command palette of `bd` diagnostics, and a
> browser for the agent memories `bd` stores. Everything goes through the `bd`
> CLI — it's a UI, not a fork.
>
> Needs Neovim ≥ 0.10 + telescope + plenary. Repo:
> https://github.com/tomfordweb/beads.nvim — feedback welcome.

**Shorter variant** (if the thread is a tight list of one-liners):

> **beads.nvim** — Telescope UI for `bd` (picker, detail view, dependency
> graph, epics, agent-memory browser), Neovim ≥ 0.10. UI over the `bd` CLI, not
> a fork. https://github.com/tomfordweb/beads.nvim

---

## Etiquette

- Match the thread's format — if everyone's posting one-liners, post the short
  variant; if they're posting paragraphs + a screenshot, post the long one with
  one GIF.
- Credit `bd` in the first or second sentence (done).
- If `fancypantalons/nvim-beads` (or any other nvim UI) is in the thread, a
  friendly nod is fine ("a couple of us are on the Neovim side now"). Never a
  comparison that knocks theirs.
- Drop one asset max (the triage GIF). Don't flood the thread.
