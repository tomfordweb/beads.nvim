# Neovim Discord — show-and-tell blurb scaffold

**Stage:** feedback. Target: general Neovim community Discord(s) → the
`#plugins` / `#show-and-tell` style channels. **No official *Beads* Discord
exists** — these are general nvim spaces. Read each server's self-promo rules
first; post once, in the right channel.

**Format:** 2-4 sentences + one GIF/screenshot + repo link. Short. Discord is
not Reddit — no walls of text.

---

## Draft (tune the voice; keep it tight)

> Built **beads.nvim** — a Telescope UI for [`bd`](https://github.com/gastownhall/beads),
> the dependency-aware issue tracker for AI coding agents. Picker with live
> filter cycling, a floating detail view (deps, epics, change history), a
> dependency graph, and a browser for the memories your agents leave behind.
> Thin async wrapper over the `bd` CLI — one fetch, client-side filtering.
> Early but usable, feedback very welcome 🙏
> `https://github.com/tomfordweb/beads.nvim`

**Even shorter (if the channel is fast-moving):**

> beads.nvim: a Telescope front-end for the `bd` issue tracker — picker, detail
> view, dependency graph, agent-memory browser. Feedback welcome →
> `https://github.com/tomfordweb/beads.nvim`

---

## Attach

- One GIF (triage loop) **or** the detail-view screenshot. Pick the single most
  legible asset — Discord previews are small.

## Notes

- Credit `bd` in the first sentence (done above).
- If someone mentions `nvim-beads`, be gracious: "yep, different plugin —
  different repo name and scope; mine leans into the richer UI side."
- Drop the requirements (nvim ≥ 0.10, telescope + plenary + `bd`) only if asked
  — keep the opener clean.
