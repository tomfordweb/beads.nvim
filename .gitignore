
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Live beads working data — never publish personal tracking.
# Tests use synthetic fixtures under tests/fixtures/ instead.
/.beads/
